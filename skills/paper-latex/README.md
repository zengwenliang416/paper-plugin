# Paper LaTeX

LaTeX track for the paper lifecycle: export a manuscript to LaTeX for English journal/conference submission (venue templates), and diagnose LaTeX compile errors.

Runs alongside the DOCX track — DOCX stays the path for Chinese graduation theses; LaTeX is for English submission and does not replace it. Export reuses Pandoc; diagnostics parse build logs (no bundled TeX distribution).
